# Skeleton asset drop

Drop the skeleton GLB in this folder as:

`skeleton.glb`

Nothing in the game loads or uses it yet. This folder is only the intake location so we can inspect the model, rig, animations, scale, orientation, and performance before wiring it into Diablo.
